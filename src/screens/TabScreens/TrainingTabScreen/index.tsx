import React from "react";
import { ScrollView, View, Text } from "react-native";


export const TrainingTabScreen = () => {

    return(
        <ScrollView>
            <Text>Página de treinos</Text>
        </ScrollView>
    )
}
import React from "react";
import { ScrollView, View, Text } from "react-native";


export const ScheduleTabScreen = () => {

    return(
        <ScrollView>
            <Text>Página de Horários agendados</Text>
        </ScrollView>
    )
}